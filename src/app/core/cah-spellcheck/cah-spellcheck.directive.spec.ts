import { CahSpellcheckDirective } from './cah-spellcheck.directive';

describe('CahSpellcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CahSpellcheckDirective();
    expect(directive).toBeTruthy();
  });
});
